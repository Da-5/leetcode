class Solution:
    def xorOperation(self, n: int, start: int) -> int:
        result = 0;
        nums = [0]*n
        for i in range(n):
            nums[i]= start + i*2

        for i in range(n):
            result = result^nums[i];
        return result
