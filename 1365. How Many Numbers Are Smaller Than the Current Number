class Solution:
    def smallerNumbersThanCurrent(self, nums: List[int]) -> List[int]:
        a=[]
        num=sorted(nums)
        for i in nums:
            a.append(num.index(i))
        return a
